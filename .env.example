# .env.example — Required secrets for the ADR Slack Bot
# Store these in Supabase Vault (production) or supabase/seed.sql (local dev).
# Edge Functions read from Deno.env; SQL functions read from vault via get_secret().

# Slack App credentials (from https://api.slack.com/apps)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret

# GitHub PAT with 'repo' scope (for git export)
GITHUB_TOKEN=ghp_your_token
GITHUB_REPO_OWNER=myorg
GITHUB_REPO_NAME=architecture
GITHUB_DEFAULT_BRANCH=main

# Supabase self-reference (outbox calls Edge Functions via pg_net)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
